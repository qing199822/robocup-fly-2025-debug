# look_up

该节点实现了一种查询服务：当追踪节点要锁定目标时，会先查询该目标是否被其他无人机锁定，当返回值为False时，无人机就开始跟踪该目标，并将这个目标置为True，此时其他无人机即使再发现这个目标也不会触发追踪；当目标消失时请求重置为False；这样就实现了一个目标同一时间只能被一架无人机锁定

spawn_mux_swarm.launch 是一个总控节点，用于在导航控制和跟踪控制之间切换